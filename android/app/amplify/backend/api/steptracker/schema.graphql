type Device @model {
  id: ID!
  name: String!
  steps: [Step] @connection(name: "DeviceSteps")
}
type Step @model {
  id: ID!
  steps: Int!
  timestamp:
  device: Device @connection(name: "DeviceSteps")
}
